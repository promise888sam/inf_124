4. Ajax autocomplete, with jQuery and MySQL, is used at input email and state.
	- For email, it dynamically gives users the options to click on a drop down box
	  which contains user's input following by some major domains.

	- For state, similarly, it dynamically gives users the options to select a 
	  complete state's name.